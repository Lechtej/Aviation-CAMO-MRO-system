## v0.2.50 — DMS scripts patch (additive)

- Added missing dev scripts to repo path:
  - scripts/dev/get_token_dev.sh (token from API container OIDC_ISSUER)
  - scripts/dev/smoke_dms.sh (smoke: /v1/dms/types + /v1/dms/documents with X-Tenant-ID)
- No breaking changes. Pure additive files.
