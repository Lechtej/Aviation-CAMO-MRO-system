name: Security

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

jobs:
  deps_scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Add dependency scanning in v0.2+"
